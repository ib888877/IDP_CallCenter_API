from django.db import models

# We are not using a database model for beneficiaries,
# since data comes directly from CSV. This file can remain empty,
# or we could create a model for logging or other minor features if needed.
